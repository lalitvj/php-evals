# Quickstart (Laravel)

1. Register `PhpEvals\\Laravel\\LaravelEvalsServiceProvider`.
2. Publish config with tag `ai-evals-config`.
3. Configure `model_client` in `config/ai-evals.php`.
4. Add suite files and run `php artisan ai:eval`.
