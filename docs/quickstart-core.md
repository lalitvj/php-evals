# Quickstart (Core)

1. Install dependencies.
2. Create `php-evals.php` with `dataset_path` and `model_client`.
3. Add suite file in JSONL format.
4. Run `packages/core/bin/php-evals`.

Exit codes:
- `0`: all passed
- `1`: assertion failures
- `2`: configuration/runtime errors
